# uchi

Dotfiles Literate Configuration CLI tool.

The command entrypoint is `cmd/uchi`; application-only code lives under
`internal/`. Templates are bundled with the binary and can be overridden from a
directory configured with `template_dir` or `--template-dir`.

## Build & Usage

Build, test, and template usage are documented in [BUILD.md](BUILD.md).
